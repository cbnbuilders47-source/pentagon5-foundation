# Test Fixtures

## NOT IMPLEMENTED

Fixture-generation tooling is not implemented.

## Milestone 2 fixtures

- `valid/domains.json` covers every required domain.
- `valid/messages.json` covers every message category.
- `invalid/messages.json` captures rejected UUID, timestamp, decimal, idempotency, strict-property, and version cases.
- `backward-compatible/v1.0.0.json` is a frozen v1 compatibility fixture.

Fixtures are contract examples, not production seed data.
