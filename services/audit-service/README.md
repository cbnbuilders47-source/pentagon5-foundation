# Audit Service

## NOT IMPLEMENTED

This directory reserves the `audit-service` service boundary. Milestone 1 defines no audit events, persistence, APIs, deployment, or mock behavior. The future service must remain independently operable.
