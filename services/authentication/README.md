# Authentication

## NOT IMPLEMENTED

This directory reserves the `authentication` service boundary. Milestone 1 defines no authentication flows, endpoints, storage, deployment, or mock behavior. The future service must remain independently operable.
