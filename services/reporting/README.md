# Reporting

## NOT IMPLEMENTED

This directory reserves the `reporting` service boundary. Milestone 1 defines no reports, generation logic, storage, deployment, or mock behavior. The future service must remain independently operable.
